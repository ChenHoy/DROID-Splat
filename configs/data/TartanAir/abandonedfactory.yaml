defaults:
  # NOTE we need to use an absolute path here, because otherwise it will not be cfg.data in the Dict
  - /data/TartanAir/base@_here_

input_folder: /media/data/TartanAir/abandonedfactory/Easy/P000/

# Because the sequence is so long, we should keep the resolution pretty low
cam:
  H_out: 240
  W_out: 320